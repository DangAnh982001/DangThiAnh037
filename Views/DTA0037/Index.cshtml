@model IEnumerable<DangThiAnh037.Models.DTA0037>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DTAId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DTAName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DTAGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DTAId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DTAName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DTAGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DTAId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DTAId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DTAId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
